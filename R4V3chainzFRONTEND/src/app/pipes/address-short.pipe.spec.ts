import { AddressShortPipe } from './address-short.pipe';

describe('AddressShortPipe', () => {
  it('create an instance', () => {
    const pipe = new AddressShortPipe();
    expect(pipe).toBeTruthy();
  });
});
